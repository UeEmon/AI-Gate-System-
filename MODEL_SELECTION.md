# モデル選択と起動時の性能測定

認識開始フォームで車両モデル、OCR方式、入力サイズ、処理間隔を選択できます。
車両モデルはYOLO11n、YOLO26n、YOLO26sおよびモデル保存ディレクトリ直下の
学習済み `.pt` ファイルから選びます。車両用重みのみ選択してください。
プレート検出専用重みは従来どおりGATE_PLATE_MODELで指定します。

起動時にCPU数、メモリ上限、約0.25秒の固定演算テストを測定します。
Dockerのcgroup v2 CPU・メモリ制限も考慮します。
4コア以上、6GiB以上、演算スコア1000以上の場合はYOLO26s/960px/毎フレーム、
それ以外はYOLO26n/640px/3フレームごとを推奨します。
同時カメラ数はそれぞれ2台・1台です。GATE_MAX_CAMERASの明示設定を優先します。
OCRの初期選択はEasyOCRです。評価に基づく選択を使う場合は画面でautoを選びます。

これはCPU向けの推奨値であり、実モデルの速度や精度を測定した最適値ではありません。
GPUは自動選択しません。プロセス起動ごとに再測定し、利用者の選択は各処理に適用します。
選択内容と測定値は各処理のsettings.jsonへ記録されます。
初回の未取得モデルは認識開始時にダウンロードされます。

Dockerへの反映は `docker compose --env-file onprem/.env -f onprem/compose.yaml up -d --build`。
既存コンテナのrestartだけでは新しいコードは反映されません。
